import React from "react";

function Card(props){
    const imageLink=props.imageLink;


    return(
            <div >
            {/* <img src={imageLink} style={cardStyle} alt=''/> */}
        </div>
    );
}

export default Card;